Analyse — Prochaines étapes ML (Session 2)
Voici ce que j'identifie comme axes d'amélioration, dans l'ordre de priorité pédagogique :

ETAPE 6 — Feature Engineering enrichi

Ratio relatif gap_evollis / total_price (% d'erreur) plutôt que le gap absolu → moins sensible aux gros montants
Encodage du product_name (LabelEncoder → nouvelle feature) → le modèle apprend que certains produits sont plus souvent erronés dans un système
Feature fournisseur si disponible
Features temporelles (mois, trimestre) pour détecter des dérives saisonnières
ETAPE 7 — Métriques de performance formelles

Actuellement : aucune métrique, validation visuelle uniquement
Ajouter : confusion matrix, F1-score, ROC-AUC, rapport de classification
Calibration des probabilités (p_origin_evollis / p_origin_hybris)
Train/test split sur les V2 ~5000 lignes
ETAPE 8 — Seuil de confiance (Règle 7 corrigée)

Actuellement : toute prédiction KO est appliquée sans garde-fou
Ajouter : if max(p_evollis, p_hybris) > 0.75 → appliquer sinon → "incertain, révision humaine"
C'est LE point de gouvernance métier manquant
ETAPE 9 — Hyperparameter tuning

GridSearch / Optuna sur max_depth, learning_rate, subsample, colsample_bytree
Early stopping sur un jeu de validation
ETAPE 10 — Explicabilité (SHAP)

Pourquoi le modèle a décidé EVOLLIS ou HYBRIS ?
SHAP values par prédiction → visualisation pour les métiers
Maintenant je crée le Step 1 du RAG POC.

Voilà ce qui a été fait. Voici le récapitulatif complet :

Session 2 ML — Prochaines étapes recommandées
Étape	Thème	Apport pédagogique
ETAPE 6
Feature Engineering enrichi (ratio %, product_name encodé, features temporelles)
"Plus une feature est informative, moins le modèle a besoin d'exemples"
ETAPE 7
Métriques formelles (F1, ROC-AUC, confusion matrix, train/test split)
Actuellement il n'y a aucun indicateur objectif de performance
ETAPE 8
Seuil de confiance (if proba > 0.75)
Corrige la Règle 7 dangereuse identifiée dans le README — c'est le point de gouvernance manquant
ETAPE 9
Hyperparameter tuning (Optuna / GridSearch)
Montre que le meilleur modèle vient de la recherche systématique, pas de l'intuition
ETAPE 10
SHAP values — explicabilité
"Pourquoi le modèle a dit HYBRIS ?" → indispensable pour la confiance métier
RAG POC — Step 1 créé
Trois fichiers dans RAG_POC/ :

RAG_ETAPE1_indexation.py — Pipeline complet : chargement de 11 documents (README, commentaires, 5 scripts Python), chunking à 500 chars avec overlap, embedding avec paraphrase-multilingual-MiniLM-L12-v2 (modèle 100% local, multilingue), stockage persistant dans ChromaDB, et 3 requêtes de vérification
requirements_rag.txt — chromadb + sentence-transformers
commentaire_RAG_etape1.txt — Explication pédagogique en français (même format que les autres commentaires d'atelier)
Pour lancer :

cd c:\Users\WCXV8632\workspace\aterlier_presentation_ml\RAG_POC
pip install -r requirements_rag.txt
python RAG_ETAPE1_indexation.py
La base vectorielle sera créée dans RAG_POC/chroma_db/. Les étapes 2 (Retrieval) et 3 (Generation avec LLM) viendront compléter le POC.